<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_order_form_auth_inherit" model="ir.ui.view">
        <field name="name">sale.order.form.auth.inherit</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">

            <xpath expr="//header" position="inside">
                <button name="action_create_delivery_auth_request"
                        string="Solicitar Entrega"
                        type="object"
                        class="btn-secondary"
                        icon="fa-lock"
                        invisible="state != 'sale' or delivery_auth_state in ('paid', 'authorized')"/>

                <!-- Estados de autorización FUERA de la vista por pedido
                     (2026-08-15): el campo queda invisible porque las alertas,
                     el botón Solicitar Entrega y el candado de entregas lo
                     usan en sus expresiones; la lógica sigue intacta. -->
                <field name="delivery_auth_state" invisible="1"/>
            </xpath>

            <xpath expr="//sheet" position="before">
                <div class="alert alert-warning" role="alert"
                     invisible="delivery_auth_state in ('paid', 'authorized') or state != 'sale'">
                    <strong>⚠ Atención:</strong> Esta orden no está pagada totalmente. Requiere autorización para entrega.
                </div>
                <div class="alert alert-success" role="alert"
                     invisible="delivery_auth_state != 'paid'">
                    <strong>✓ Listo:</strong> Orden pagada al 100%. Entrega autorizada automáticamente.
                </div>
                <div class="alert alert-info" role="alert"
                     invisible="delivery_auth_state != 'authorized'">
                    <strong>✓ Autorizado:</strong> Entrega autorizada manualmente.
                </div>
            </xpath>

            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_delivery_auth_requests"
                        type="object"
                        class="oe_stat_button"
                        icon="fa-check-circle"
                        invisible="delivery_auth_request_count == 0">
                    <field name="delivery_auth_request_count" widget="statinfo"
                           string="Autorizaciones"/>
                </button>
            </xpath>

        </field>
    </record>
</odoo>